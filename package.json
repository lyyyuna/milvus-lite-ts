{
  "private": true,
  "scripts": {
    "build": "cd packages/milvus-lite && npm run build",
    "test": "cd packages/milvus-lite && npm test",
    "fetch-binaries": "bash scripts/fetch-binaries.sh"
  }
}
